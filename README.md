# Build
```
./gradlew buildPlugin
```

# TODO
1. Stop sending audio when it's below human speech amplitude
2. Fix "code blocks not block-level elements"
3. Clean up log messages
4. Allow interruption _w/o push-to-interrupt_ (echo cancellation)

